node_modules/
dist/
.env
.dev-server.pid
.dev-server.log
.api-server.pid
.api-server.log

# built frontend copied into the server for deploy
public/

# SQLite database (created at runtime / on the Render disk)
*.db
*.db-shm
*.db-wal
*.sqlite
