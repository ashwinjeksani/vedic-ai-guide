node_modules/
dist/
.env
.dev-server.pid
.dev-server.log
.api-server.pid
.api-server.log

# built frontend copied into the server for deploy (repo-root only,
# so frontend/public/ source assets like favicon.svg stay tracked)
/public/

# SQLite database (created at runtime / on the Render disk)
*.db
*.db-shm
*.db-wal
*.sqlite

# deployment docs (kept local); render.yaml is tracked for the Render Blueprint
DEPLOY.md
