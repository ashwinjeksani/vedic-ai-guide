import React from "react";
import { createRoot } from "react-dom/client";
import SanatanaGuide from "../vedic-guide.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SanatanaGuide />
  </React.StrictMode>
);
